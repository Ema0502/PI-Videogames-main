
const initialState = {};

const reducer = (state = initialState, { type, payload }) => {
  switch (type) {
    case type:
      
      break;
  
    default:
      return {
        ...state,
      }
  }
}
 export default reducer;